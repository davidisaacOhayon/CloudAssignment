service: backend
runtime: custom
env: flex

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 10
  cpu_utilization:
    target_utilization: 0.6
    
beta_settings:
  cloud_sql_instances: davidassignment:europe-west1:backenddb